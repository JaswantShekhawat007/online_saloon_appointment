server:
  port: 8085
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/saloon_service_db
    username: postgres
    password: Capg@52898
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  #h2:
    #console:
    #  enabled: true
